# ---------------------------------------------------------------------------
# Base (runtime) dependencies — required in every environment.
# Pinned with lower bounds; regenerate a full lock file (pip-tools/uv) per
# deployment for reproducible builds.
# ---------------------------------------------------------------------------

# Web framework & ASGI server
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
uvicorn-worker>=0.3.0
gunicorn>=23.0.0

# Data validation & settings
pydantic>=2.9.0
pydantic[email]>=2.9.0
pydantic-settings>=2.6.0

# Database (async + sync drivers) & migrations
SQLAlchemy>=2.0.36
alembic>=1.14.0
asyncpg>=0.30.0
psycopg[binary]>=3.2.0

# Cache / broker
redis>=5.2.0

# Background tasks (Beat scheduler ships with celery)
celery>=5.5.0

# File uploads (multipart parsing) & async file I/O
python-multipart>=0.0.18
aiofiles>=24.1.0

# PDF manipulation (watermarks, overlays, encryption) & image-to-PDF
pypdf>=6.0.0
cryptography>=44.0.0    # AES-256 encryption backend for pypdf
reportlab>=4.2.0        # overlay/watermark/page-number rendering
img2pdf>=0.6.0          # lossless image -> PDF embedding
Pillow>=11.0.0
pdf2docx>=0.5.8         # PDF -> editable DOCX (layout reconstruction)
pikepdf>=9.0.0          # PDF repair/decrypt pre-flight before conversion
PyMuPDF>=1.24.0         # PDF introspection & fallback DOCX extraction
ocrmypdf>=17.8.0        # searchable text layer for scans (needs Tesseract + Ghostscript)

# Authentication
PyJWT>=2.10.0
bcrypt>=4.2.0

# Structured logging
structlog>=24.4.0

# Environment file loading
python-dotenv>=1.0.1

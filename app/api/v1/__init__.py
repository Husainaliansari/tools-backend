"""API version 1 package.

Versioning the API by package (``v1``, ``v2``, ...) lets breaking changes ship
as a new version while old clients keep working — a core requirement for
long-lived services. Feature endpoint modules live under this package.
"""
